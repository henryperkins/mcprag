{
  "mcpServers": {
    "azure-code-search": {
      "command": "python",
      "args": [
        "-m",
        "mcprag"
      ],
      "env": {
        "ACS_ENDPOINT": "${ACS_ENDPOINT}",
        "ACS_ADMIN_KEY": "${ACS_ADMIN_KEY}",
        "ACS_INDEX_NAME": "${ACS_INDEX_NAME:-codebase-mcp-sota}"
      }
    }
  }
}
